name: Build Client
on: push

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v2

      # Add node dependency
      - name: Use Node.js 16.x
        uses: actions/setup-node@v2
        with:
          node-version: 16.x
          cache: "npm"
          cache-dependency-path: package-lock.json

      # Set adress for online connection
      - run: echo "export const SERVER_LINK = 'wss://games-on-web-2022.herokuapp.com/';" > src/clients/connection/server_address.ts

      # Adding node modules
      - run: npm install -g typescript;
      - run: npm -f ci;

      # Preparing the build
      - run: node src/switchServerClient.js t c src/index.tsx;
      - run: npm run build
        env:
          CI: ""

      # Push to branch
      - name: Push
        uses: s0/git-publish-subdir-action@develop
        env:
          REPO: self
          BRANCH: branch-client # The branch name where you want to push the assets
          FOLDER: build # The directory where your assets are generated
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # GitHub will automatically add this - you don't need to bother getting a token
          MESSAGE: "Build: ({sha}) {msg}" # The commit message
